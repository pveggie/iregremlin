sudo: false
language: ruby
rvm:
  - 2.3.3

services:
  - postgresql

before_script:
  - psql -c 'create database iregremlin_test;' -U postgres

test:
  adapter: postgresql
  database: iregremlin_test

script:
  - export RAILS_ENV=test
  - bundle exec rake db:create db:migrate

deploy:
